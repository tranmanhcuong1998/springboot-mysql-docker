spring:
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/productdb
    username: root
    password: 17111998a@A
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8080